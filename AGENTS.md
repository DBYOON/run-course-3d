# AGENTS.md

## 프로젝트 개요

- 이 저장소는 러너들을 위한 `RunCourse3D` 프로젝트다.

이 프로젝트는 React Native 기반의 네이티브 앱 컨테이너와, React + TypeScript 기반의 WebView 콘텐츠로 구성된 하이브리드 애플리케이션이다.


### 기술 스택

- React Native 0.86.3
- Expo SDK 57
- TypeScript
- **화면 탐색**: Expo Router
- **WebView**: react-native-webview
- **지도(Map) 렌더링**: MapLibre GL JS (WebView)
- **GPS 및 위치 권한**: expo-location
- **백그라운드 GPS**: expo-task-manager
- **3D 러너 렌더링**: Three.js
- **상태 관리**: Zustand
- **서버 상태 관리**: TanStack Query
- **로컬 데이터베이스**: Expo SQLite
- **서버 데이터베이스**: PostgreSQL + PostGIS
- **백엔드**: Supabase


## 작업 기본 원칙

### 최소 범위 수정

요청사항을 해결하기 위해 필요한 코드만 수정한다.

다음 행동은 하지 않는다.

- 관련 없는 코드 리팩터링(리팩토링)
- 요청과 관계없는 변수명 변경, 파일 이동
- 전체 코드 포맷 변경
- 필요하지 않은 구조 개선

### 기존 패턴 우선

Codex 개인의 선호보다 현재 프로젝트의 구현 방식을 우선한다.

- 가능하면 기존의 컴포넌트, 훅, API 함수, 유틸리티, 타입을 재사용한다.
- 새로운 아키텍처를 도입하지 않는다.

### 과도한 설계 금지

다음을 피한다.

- 실제 필요가 없는 추상화
- 한 번만 사용하는 공통 함수
- 미래 사용을 예상한 설계
- 불필요한 디자인 패턴 적용
- 불필요한 의존성 추가

## 작업 시작 전 확인

코드를 수정하기 전에 반드시 다음을 확인한다.

1. 요구사항과 관련된 파일을 찾는다.
2. 해당 파일의 기존 구현을 읽는다.
3. 해당 코드가 어디에서 사용되는지 확인한다.
4. 관련 타입, 훅, API, 유틸리티를 확인한다.
5. 비슷한 기능을 구현한 기존 코드를 찾는다.
6. 변경으로 인해 영향을 받을 수 있는 영역을 확인한다.
7. 가장 작은 수정 방법을 결정한다.

존재 여부를 확인하지 않은 파일, 함수, API, 라이브러리를 있다고 가정하지 않는다.

## TypeScript 규칙

- `any` 사용을 피한다.
- 기존 타입 또는 DTO가 있으면 재사용한다.
- 동일한 타입을 중복 선언하지 않는다.
- `null`, `undefined` 가능성을 고려한다.
- 가능한 경우 명확한 Union Type을 사용한다.

다음 방식으로 TypeScript 오류를 임의로 무시하지 않는다.

- `as any`
- `@ts-ignore`
- `@ts-expect-error`

불가피하게 사용하는 경우 이유를 설명한다.

## React 규칙

**컴포넌트 작성**
- 모든 컴포넌트는 함수형 컴포넌트(Functional Component)로 작성한다.
- 기존 프로젝트의 컴포넌트 구조 및 디렉토리 패턴을 엄격히 따른다.

**상태 관리 (State Management)**
- **파생된 상태(Derived State) 금지:** props나 기존 state로 계산 가능한 값은 별도 state를 만들지 않고 렌더링 과정에서 직접 계산한다.
- Props Drilling은 최대 깊이 3~4단계까지만 허용하고, 그 이상은 지양한다.
- state 업데이터 작성 시 불변성을 철저히 지킨다.

**최적화 및 Side Effect**
**useEffect는 '서버/브라우저 연동'에만 사용:** 데이터 조회(Data Fetching), 구독(Subscription), DOM 제어 등 외부 시스템과의 동기화 목적 외에는 사용하지 않는다. 버튼 클릭, 폼 제출 등 사용자 행동에 따른 작업은 이벤트 핸들러(onClick, onSubmit 등)에서 직접 처리한다.
- **useMemo / useCallback 제한:** 성능 병목이 실제로 발생하거나 참조 동일성(Referential Equality) 보장이 필수적인 경우에만 선택적으로 사용한다.
- Side Effect 및 비동기 로직은 Custom Hook 또는 이벤트 핸들러 내부로 명확히 분리한다.

**리스트 및 이벤트 규칙**
- 리스트 렌더링 시 `key` 값으로 배열 인덱스(`index`) 사용을 금지하며, 고유한 ID를 부여한다.
- 이벤트 핸들러 전달 및 작성 시 네이밍 규칙을 준수한다. (Props: `onXxx`, Handler: `handleXxx`)


## 데이터 요청(API / TanStack Query) 규칙

서버 상태는 프로젝트에서 사용하고 있는 React Query 패턴을 따른다.

- 기존 Query Key를 재사용한다.
- 기존 API 함수를 재사용한다.
- 동일 API 요청을 중복 구현하지 않는다.
- 서버 데이터를 불필요하게 useState에 복사하지 않는다.
- Mutation 이후 invalidate 또는 cache 업데이트는 기존 패턴을 따른다.

## 아키텍처 규칙

현재 프로젝트의 디렉토리 구조와 의존성 규칙을 따른다.

명시적인 요청 없이:
- 파일을 다른 레이어로 이동하지 않는다.
- 새로운 레이어를 만들지 않는다.
- 프로젝트 구조를 변경하지 않는다.

다음을 피한다.

- 순환 의존성
- 허용되지 않은 레이어 간 직접 참조
- 동일한 도메인 로직 중복
- feature 전용 로직을 shared에 배치하는 것

## 외부 라이브러리

새로운 라이브러리를 추가하기 전에 다음을 확인한다.

- 프로젝트 내부에 이미 동일한 기능이 있는지 확인한다.
- 현재 설치된 라이브러리로 해결 가능한지 확인한다.
- 간단한 구현으로 해결 가능한지 확인한다.
- 추가 의존성이 실제로 필요한지 판단한다.

명시적인 요청 없이 기존 라이브러리를 다른 라이브러리로 교체하지 않는다.

## 테스트 및 검증

코드를 수정한 것만으로 작업이 완료된 것으로 판단하지 않는다.

작업 후 변경 범위에 맞는 검증을 수행한다.

### 기본 명령어

```bash
# 의존성 설치
pnpm install

# 정적 검증
pnpm lint         # ESLint 실행
pnpm typecheck    # TypeScript 타입 검사

# 테스트
pnpm test         # 테스트 실행
pnpm stage-lint   # staged 파일만 빠른 lint

# 빌드
pnpm run build
```

### 검증 원칙

* 변경 범위와 관련된 가장 작은 검증부터 실행한다.
* 필요한 경우 전체 lint, test, build를 실행한다.
* 실제로 실행하지 않은 검증에 대해 성공했다고 말하지 않는다.
* 검증을 실행하지 못했거나 실패한 항목이 있다면 작업 완료 보고에 명확하게 작성한다.
* 단순 코드 확인만으로 "정상 동작", "빌드 성공", "테스트 통과"라고 판단하지 않는다.

## 변경 금지 사항

다음 항목은 사용자의 요청이 없는 경우 변경하지 않는다.

- package.json dependency 버전
- package manager
- tsconfig
- ESLint 설정
- Prettier 설정
- Vite/Webpack 설정
- CI/CD
- 디렉토리 구조
- 아키텍처
- 환경변수 구조

## 작업 완료 조건

작업 완료 전에 확인한다.

- [ ] 요청한 기능이 구현되었는가
- [ ] 기존 프로젝트 패턴을 따르고 있는가
- [ ] 관계없는 코드를 수정하지 않았는가
- [ ] TypeScript 오류를 임의로 우회하지 않았는가
- [ ] 필요한 검증을 수행했는가
- [ ] 기존 기능에 영향을 줄 가능성을 확인했는가
- [ ] 검증하지 못한 부분이 있다면 명확하게 설명했는가

## 부칙

- 사용자와의 대화 및 작업 결과 설명은 한국어로 작성한다.
- 코드, 파일명, 식별자에는 프로젝트의 기존 언어와 명명 규칙을 따른다.
- 비밀 키, 토큰, 인증 정보, 개인 데이터는 코드나 문서에 기록하지 않는다.
- AI 처리 과정 메시지와 응답 메시지는 모두 한국어로 작성한다.
- 처리 과정 메시지를 제외한 모든 응답에는 존댓말을 사용한다.
- 응답에 한글 인코딩 깨짐 또는 타 언어 혼입이 보이면 즉시 한국어 문장으로 정정한다.
    - 응답 생성 시 **한글(UTF-8) 인코딩이 깨지지 않도록 확인**하고, 깨진 문자열(예: `��`) 또는 타 언어 문장이 섞이면 즉시 전체 문장을 한국어로 다시 작성한다.
- 한국어로 주석 작성하고 내용이 바뀌면 주석도 변경한다.
- 사용자가 요청한 작업 범위외 파일은 수정하지 않는다.
- 이해하지 못한 요구 사항이 있으면 반드시 사용자에게 되물어본다.
- 디버그 로그는 문제가 해결 되었다면 반드시 삭제한다.
- 보안에 취약한 코드는 특별한 지시가 없다면 사용 금지한다.
- 추측성 수정은 절대 금지한다. 원인을 모르면 수정 금지한다.
- 간단하지 않은 문제라면 웹 검색을 통해 최신 해결책을 검색한다.
- 이 프로젝트는 프로덕션이다. 오류 수정을 위해 우회하는 임시 코드를 작성 금지한다.
- 코드 작업 시 `docs/` 폴더의 모든 Markdown 문서를 숙지하고, 해당 문서에 정의된 규칙을 준수한다. `docs` 폴더가 없을 경우 이 지침은 무시해도 된다.
